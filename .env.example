# =============================================================================
# agent_auto_learn — Root environment
# =============================================================================
# This file is a TEMPLATE. Copy it to `.env` and fill in your own values:
#
#     cp .env.example .env
#
# The real `.env` is gitignored and must NEVER be committed.
# =============================================================================

# ---- LLM Provider ----------------------------------------------------------
# OpenRouter key — used by backend scripts when run from the repo root.
# Get a key here:  https://openrouter.ai/keys
# Format: sk-or-v1-<64-char-hex>
OPENROUTER_API_KEY="sk-or-v1-replace_me_with_your_real_key"
